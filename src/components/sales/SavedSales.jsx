import React from 'react'
import { useEffect } from 'react'

export default function SavedSales({ sales }) {
    // const [savedSales, setsavedSales] = useState([...sales]);
    useEffect(() => {
        // setsavedSales(localStorage.getItem())
        return () => {
            
        };
    }, []);
  return (
    <div>
      
    </div>
  )
}
