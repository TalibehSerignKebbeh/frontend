import React from 'react';
import SearchProducts from './Select/SearchProducts';

const RegisterSale = () => {
    return (
        <div>
            <SearchProducts />
        </div>
    );
}

export default RegisterSale;
